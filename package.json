{
  "name": "workpacket",
  "version": "0.1.0",
  "description": "Compiler-style pipeline that converts assignment materials into structured Execution Packets",
  "type": "module",
  "bin": {
    "workpacket": "./src/cli/main.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:integration": "RUN_LIVE_LLM_TESTS=1 bun test integration",
    "test:all": "bun test && RUN_LIVE_LLM_TESTS=1 bun test integration",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "cli": "bun run src/cli/main.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "dependencies": {
    "zod": "^3"
  }
}
