{
  "name": "workpacket",
  "version": "0.1.0",
  "description": "Compiler-style pipeline that converts assignment materials into structured Execution Packets",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "dependencies": {
    "zod": "^3"
  }
}
